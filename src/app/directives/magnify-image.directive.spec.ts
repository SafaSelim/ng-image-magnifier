import { MagnifyImageDirective } from './magnify-image.directive';

describe('MagnifyImageDirective', () => {
  it('should create an instance', () => {
    const directive = new MagnifyImageDirective();
    expect(directive).toBeTruthy();
  });
});
